name: 02 Vanilla Image

jobs:
   build_vanilla_image:
    runs-on: ubuntu-latest 
    env:
      image_output: ""
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      actions: read
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v3

      - name: Download artifact
        uses: dawidd6/action-download-artifact@v2
        with:
          github_token: ${{secrets.GITHUB_TOKEN}}
          workflow: user_image.yaml
      
      - name: 'Run packer + ansible'
        run: |
          cat user-image-version/artifact.txt
          echo this should call build_vanilla.sh
          echo "win10-packer-vanilla-$(date +'%Y%m%d%H%M%S')" > artifact.txt
      
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: vanilla-image-version
          path: artifact.txt
